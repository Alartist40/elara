# Core LLM
llama-cpp-python>=0.3.0

# RAG
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# API (optional)
openai>=1.0.0

# STT - OLMoASR (Apache 2.0, fully open)
olmoasr>=0.1.0

# TTS - Piper (fast, local)
piper-tts>=1.2.0

# Audio processing
numpy>=1.24.0
soundfile>=0.12.0
sounddevice>=0.4.0

# Utils
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0
python-dotenv>=1.2.0
psutil>=5.9.0

# Optional: For better TTS quality
# librosa>=0.10.0  # Uncomment if needed for audio processing
